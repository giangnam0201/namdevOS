#!/bin/bash
#
# Cleanup hook to reduce image size
# Removes temporary files, apt cache, and unnecessary data
#

set -euo pipefail

echo "Running cleanup..."

# Clean apt cache
apt-get clean
apt-get autoremove -y --purge

# Remove apt package lists (will be regenerated on first apt update)
rm -rf /var/lib/apt/lists/*

# Remove temporary files
rm -rf /tmp/*
rm -rf /var/tmp/*

# Remove log files
find /var/log -type f -name "*.log" -delete
find /var/log -type f -name "*.gz" -delete

# Remove documentation to save space (optional, comment out to keep)
# rm -rf /usr/share/doc/*
# rm -rf /usr/share/man/*

# Remove cached font data
rm -rf /var/cache/fontconfig/*

# Remove bash history
rm -f /root/.bash_history

echo "Cleanup complete"
